name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run:  docker build -t myavaloniaapp .
            docker tag myavaloniaapp vaynemai021296/myavaloniaapp:latest
            docker push vaynemai021296/myavaloniaapp:latest
